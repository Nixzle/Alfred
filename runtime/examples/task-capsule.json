{
  "schema_version": 1,
  "objective": "Fix the bounded login regression",
  "scope": {
    "allowed_targets": ["src/login.py", "tests/test_login.py"],
    "non_goals": ["authentication architecture rewrite", "dependency upgrades"]
  },
  "authority": {
    "allowed_actions": ["read", "write", "test"],
    "approval_required_actions": []
  },
  "acceptance": {
    "required_evidence": ["targeted test passes", "portable suite remains green"]
  }
}
